{
  "name": "Database Connection",
  "description": "Database provider setup for SQLite and Postgres plus transaction context helpers for application stores.",
  "entrypoints": [
    {
      "type": "library-api",
      "path": "rest2go.NewDbProvider(settings.Database)",
      "file": "./pkg/rest2go/database.go",
      "symbol": "NewDbProvider"
    }
  ],
  "files": [
    {
      "path": "./pkg/rest2go/database.go",
      "role": "main-flow",
      "priority": 1,
      "description": "Initializes the configured database driver, exposes the DB handle, and defines transaction context/store contracts."
    },
    {
      "path": "./pkg/rest2go/settings/blocks.go",
      "role": "configuration",
      "priority": 2,
      "description": "Defines database driver, host, port, credentials, name, and schema settings."
    },
    {
      "path": "./README.md",
      "role": "documentation",
      "priority": 3,
      "description": "Documents SQLite/Postgres configuration and provider usage from applications."
    }
  ]
}
